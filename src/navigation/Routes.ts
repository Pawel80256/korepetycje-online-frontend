export enum AppRoutes {
    HOME = "/",
    REGISTER = "/register",
    LOGIN = "/login",
    TEACHER_PUBLIC_PROFILE = "/teacher/:teacherId",
    CLIENT_PROFILE = "/client/:clientId",
    SEARCH = "/search"
}